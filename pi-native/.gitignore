node_modules/
panel-config.json
alarms.json
custom-cube-config.json
unsplash-config.json
nasa-config.json
weather-config.json
wall-layout-config.json
bt-config.json
uploads/
tls-key.pem
tls-cert.pem
# Build artifacts for the GitHub Pages simulator (see sim/README.md) -
# regenerated by `node sim/deploy.js` from src/, not hand-edited. The
# actual deploy output (<repo root>/pi-sim/) IS committed, same as the
# root simulator's own build.sh output - only these intermediates aren't.
public/sim-engine.js
public/effects.json
