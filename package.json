{
  "dependencies": {
    "playwright": "^1.61.1"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "three": "0.168.0"
  },
  "scripts": {
    "build:three": "esbuild build-tools/three-entry.js --bundle --minify --format=iife --global-name=THREE --outfile=three.min.js"
  }
}
